export const getAvailableTasks = async (_walletAddress: string): Promise<any[]> => {
  return [];
};

export const submitTask = async (_payload: any): Promise<any> => {
  return {};
};

export const getUserTaskHistory = async (_walletAddress: string): Promise<any[]> => {
  return [];
};
